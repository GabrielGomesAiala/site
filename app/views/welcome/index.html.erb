

<% @capas.each do |capa| %>


			<img class="teste" src="<%= capa.imagem.url(:medium) %>" alt="">

<% end %>
	
	<div class="container">
		
		</br>
		</br>
		<div class="section-title">
						<h1>Destaques</h1>
						<span class="st-border"></span>
					</div>
			<% @destaque.each do |destaque|%>
					<div class=" col-md-4">
					<div class="st-pricing text-center">
            
                        <div class="test" style="background-image: url(<%= destaque.imagem.url(:medium) %>)">
                          
                        </div>						

						<div class="st-border"></div>
						<ul>
							<li> <h5><%= destaque.nome %><h5></li>
							<li><%= destaque.tipo %></li>


						</ul>
							<p ><%= link_to "Ver mais", produto_path(destaque), class: "btn btn-send" %><p>
					</div>
				</div>
				<% end %>
			</div>		
</br>

		<% if current_user  %>
		<section id="our-team">
		<div class="container">
			<div class="row">
				<div class="col-md-12">
					<div class="section-title">
						<h1>Deletar capas</h1>
						<span class="st-border"></span>
					</div>
				</div>

				
				
				<% @capas.each do |capa| %>

				<div class="col-md-3 col-sm-6">
					<div class="team-member">
						<div class="member-image">
							<img class="img-responsive" src="<%= capa.imagem.url(:medium) %>" alt="">

						</div>
						<div class="member-info">
							<h4><%= capa.descricao %></h4>
							<p ><%= link_to "destroy", capa_path(capa.id), method: "delete", class: "btn btn-delete" %><p>
						</div>
					</div>
				</div>
				<% end %>

			</div>
		</div>
	</section>
	<% end %>
